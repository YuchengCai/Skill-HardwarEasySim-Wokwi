# LED 卡（wokwi-led）✅

- **与配对电阻同列**：LED 放其配对电阻正上方同列，A 脚 x 对准电阻列 → 信号线竖直最短
- **接线**：A ← 电阻（普通可见线）；C → bn 负轨（普通可见线，直连轨）
- **不用 $bb**：LED 不插板，悬浮在面包板上方
- **重叠检测**：视觉本体 ~16px 圆泡，检测用 20×20（不是 40×50 容器）
- 何时不适用：无配对电阻；该列被占；LED 数超可用列
